export enum OocyteState {
  VERY_IMMATURE = "very_immature",
  IMMATURE = "immature",
  MATURE = "mature",
}

export enum FertilizationTechnique {
  FIV = "FIV",
  ICSI = "ICSI",
}

export enum SemenSource {
  OWN = "own",
  DONATED = "donated",
}

export enum PgtResult {
  OK = "ok",
  NOT_OK = "not_ok",
  PENDING = "pending",
}

export enum EmbryoDisposition {
  CRYOPRESERVED = "cryopreserved",
  TRANSFERRED = "transferred",
  DISCARDED = "discarded",
}
